// Define interfaces for the frontend based on backend models

// User with id, login, email, password
export interface User {
    id: number
    login: string,
    email: string,
    password: string,
}
